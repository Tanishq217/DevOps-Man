apiVersion: v1
kind: Service
metadata:
  name: web-headless
  labels:
    app: web-headless
spec:
  clusterIP: None       # Defines this as a Headless Service
  selector:
    app: web-headless
  ports:
    - name: http
      port: 80
      targetPort: 80
