apiVersion: v1
kind: Pod
metadata:
  name: lifecycle-readiness
  labels:
    app: lifecycle-test
spec:
  containers:
    - name: nginx
      image: nginx:1.27
      ports:
        - containerPort: 80
      readinessProbe:
        httpGet:
          path: /
          port: 80
        initialDelaySeconds: 5
        periodSeconds: 5
