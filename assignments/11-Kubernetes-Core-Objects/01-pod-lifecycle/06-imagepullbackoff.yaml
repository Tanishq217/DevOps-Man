apiVersion: v1
kind: Pod
metadata:
  name: lifecycle-image-error
  labels:
    app: lifecycle-test
spec:
  containers:
    - name: broken-image
      image: non-existent-repo-12345/invalid-image:tag-xyz
