apiVersion: v1
kind: Pod
metadata:
  name: lifecycle-pending
  labels:
    app: lifecycle-test
spec:
  containers:
    - name: impossible-resource
      image: nginx:1.27
      resources:
        requests:
          cpu: "100"
          memory: "500Gi"
