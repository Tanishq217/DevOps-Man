apiVersion: v1
kind: Pod
metadata:
  name: lifecycle-succeeded
  labels:
    app: lifecycle-test
spec:
  restartPolicy: Never
  containers:
    - name: task
      image: busybox:1.36
      command: ["sh", "-c", "echo 'Batch job started'; sleep 5; echo 'Batch job completed successfully'; exit 0"]
